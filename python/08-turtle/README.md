# Черепаха

самая свежая документация доступна на английском языке [тут](https://docs.python.org/3/library/turtle.html)

Это библиотека позволяет создавать графические приложения.
И на первый взгляд простые концепции предоставляют мощьный инструментарий.

Представим что у нас есть абстракный робот-черепаха который подчиняется нашим указаниям
во ворту у этой черепахи зажато перо или кисть мы можем давать команды этому роботу
двигаться вперёд, назад, менять перо на перо другого размера, менять цвет чернил,
прижимать перо к поверхности по которой он движется или поднимать.
Порехность по которой движется робот это бесеонечная плоскость.
Можно представить что робочерепаха двигается по огромному листу бумаги,
а мы видим ограниченую его часть через окно нашей программы.

## Команды для черепашки

### Перемещение

forward(distance) | fd(distance) - движение вперёд на отрезок длинной

backward(distance) | bk(distance) | back(distance) - движение назад на отрезо длинной

right(angle) | rt(angle) - поворот направо на угол

left(angle) | lt(angle) - поворот налево на угол

goto(x, y=None) | setpos(x, y=None) | setposition(x, y=None) - перемещение в позицию

setx(x) - устанавливаем позицию по координате - X

sety(y) - устанавливаем позицию по координате - Y

setheading() | seth() - устанавливаем напрвление
| standard mode | logo mode   |
| 0 - восток    | 0 - север   |
| 90 - север    | 90 - восток |
| 180 - запад   | 180 - юг    |
| 270 - юг      | 270 - запад |

home() - перемещение черепахи в начало координат

circle(radius, extent=None, steps=None) - рисуем круг заданого радиуса
radius - радиус
extent - градус какая часть круга будет нарисована
steps - количество шагов используемое для рисования круга

dot(size=None, *color) - рисуем точку
size – размер точки. целое >= 1 (если задаётся)
color – имя цвета представленное строкой или кортеж определяющий цвет в схеме (RGB)

stamp() - ставим штамп или отпечаток соответспующий форме черепахи
в позиции в которой находится черепаха возвращает идентификатор штампа

clearstamp(stampid) - удаляет штамп с заданным идэнтификатором

clearstamps(n) - удаляем все или первые/последние n штампов
n - целое число
если n > 0 удаляем n первых штампов, иначе если n < 0 удаляем последние n штампов.

undo() - отменяем последнее действие, количество действий которые мы можем отменить
определяется размером буффера который сохраняет совершенные действия

speed() - устанавливаем скорость



### Узнаем состояние

position() | pos() - в какой позиции черепашка

towards() - узнаём угол между направлением черепашки и вектором от черепашки до точки с координатами

xcor() - какая координата по оси

ycor() - какая координата по оси

heading() - узнаём ориентацию черепашки

distance() - определение расстояния от черепашки до точки с координатами



### Setting and measurement

degrees() - устанавливает количество градусов в полном круге значение по умолчанию 360

radians() - устанавливает угловое измерение в радианы



### Управление пером

pendown() | pd() | down() - опускаем перо

penup() | pu() | up() - поднимаем перо

pensize() | width() - устанавливам ширину пера

pen() - возвращает словарь в котором определены настройки пера

isdown() - узнаём опущено или поднято перо



### Управление цветом

color() - возвращает или устанавливаем цвет пера и цвет заливки

pencolor() - возвращает или устанавливаем цвет пера

fillcolor() - возвращает или устанавливаем цвет заливки



#### Заливка цветом

filling() - возвращает состояние заливки если идёт заливка
возвращает True в противном случае возвращает False

begin_fill() - вызывается до рисования формы которая должна быть заполнена цветом

end_fill() - вызывается после окончания рисования фигуры
которая должна быть заполнена цветорм



### More drawing control

reset() - устанавливаем черепаху в начало координат и стираем все что она нарисовала

clear() - стираем все что нарисовала черепаха

write() - вывести текст в позиции в которой стоит черепаха
write(arg, move=False, align="left", font=("Arial", 8, "normal"))
arg – object to be written to the TurtleScreen
move – True/False
align – one of the strings “left”, “center” or right”
font – a triple (fontname, fontsize, fonttype)
Пример:



# Видимость

showturtle() | st() - показываем черепашку

hideturtle() | ht() - скрываем черепашку

isvisible() - узнаём спрятана ли черепашка



# Внешний вид

shape(name) - устанавливает или возвращает форму черепашки
формы доступные по умолчанию  “arrow”, “turtle”, “circle”, “square”, “triangle”, “classic”
также можно определить любую другую форму с помощью функции register_shape().

resourced(rmode) возвращает или устанавливает режим масштабирования черепахи
rmode – может принимать следующие значения “auto”, “user”, “noresize”
“auto”: адаптирует размер черепахи в зависимости от размера пера.
“user”: адаптирует размер черепахи в соответствии со значениями stretchfactor and outlinewidth (outline), которые устанавливаются функцией shapesize().
“noresize”: no adaption of the turtle’s appearance takes place.

shapesize(stretch_wid=None, stretch_len=None, outline=None) | turtlesize(stretch_wid=None, stretch_len=None, outline=None)

shearfactor(shear=None) - устанавливает или возвращает

settiltangle()

tiltangle()

tilt()

shapetransform(t11=None, t12=None, t21=None, t22=None)

get_shapepoly()



# Использование событий

onclick(fun, btn=1, add=None)

onrelease(fun, btn=1, add=None)

ondrag(fun, btn=1, add=None)



# Специальные методы

begin_poly() - начать записывать вершины полигона текущее положение черепахи считается первой вершиной

end_poly() - закончить записывать вершины полигона текущее положение черепахи считается последней вершиной полигона

get_poly() - вернуть последний записаный полигон

clone() - создать копию черепахи

getturtle() | getpen() - 

getscreen() - возвращает объект экрана

setundobuffer() - установить объем буффера отменяемых действий

undobufferentries() - возвращает количество сохраненных дейсвий в буффере отменяемых действий



# Методы экрана

bgcolor() - устанавливаем или получаем цвет фона

bgpic(picname=None) - устанавливаем изображение как фон

clear() | clearscreen() - удаляет все нарисованое всеми черепахами на экране всех черепах и приводит экран в  состояние по умолчанию (белый фон, не уснановлены фон и обработчики событий и трассировка включена)

reset() | resetscreen() - сбрасывает положение всех черепах на экране на начальное

screensize() - 

setworldcoordinates() - 



# Комтролирование анимации

delay(delay=None) - устанавливает задержку между перерисовкой в милисекундах чем больше задержка между кадрами тем медленнее анимация

tracer(n=None, delay=None) - отрисовка только каждого n-того кадра в задержкой (может быть исползована для ускорение отрисовки сложной графики)

update() - Вызывает отрисовку



# Использование событий

listen(xdummy=None, ydummy=None) -

onkey(fun, key) | onkeyrelease(fun, key) - обрабатывам отпускание клавишы

onkeypress(fun, key) - обрабатываем нажатие клавишы

onclick((fun, btn=1, add=None) | onscreenclick((fun, btn=1, add=None) - обрабатываем нажатие млавишы мышки на экране

ontimer(fun, t=0) - устанавливаем таймер, позволяет запусить функцию  через t - милисекунд

mainloop() | done() - запускает событийный цикл должен быть последней инструкцией в файле программы. Не должен быть использован при запуске скрипта из IDLE в -n режиме (без подпроцессов) - для интерактивного использования.



# Настройки

mode(mode) - 

colormode(cmode) - устанавливает способ которым мы определяем цвета
cmode - может принимать значения 1.0 или 255
тогда в схеме RGB мы задаём составляющие цвета от 0 до 1 или соответственно от 0 до 255 

getcanvas() - возвращает объект холста (полезно для тех кто в курсе как работать с Tkinter Canvas)

getshapes() - возвращает список всех доступных фигур которые может нарисовать черепаха

register_shape() | addshape() - зарегистрировать новую фигуру которую можно будет использовать

turtles() - возвращает список всех черепах на экране

window_height() - возвращает высоту экрана

window_width() - возвращает ширину экрана



# Методы ввода

textinput(title, prompt) - вызывает диалоговое окно с текстовым полем ввода
возвращает введенную ползователем строку или None в случае если окно было закрыто
title - заголовок всплывающего окна
prompt - более подробная информация которая проясняет детали того что именно требыется ввести в виде текста

numinput(title, prompt, default=None, minval=None, maxval=None) - вызывает всплывающее окно
для ввода числового значения возвращает введеное ползователем значение или None в случае если окно было закрыто
title - заголовок всплывающего окна
prompt - более подробная информация которая проясняет детали того что именно требыется ввести в виде текстаъ
default - значение по умолчанию
minval - минимальное возможное значение
maxval - максимальное возможное значение



# Методы специфичные для экрана

bye() - закрывает окно графического приложения

exitonclick() - устанавливает обработчиком события нажатия на кнопку мышы функцию bye()

setup(width=_CFG["width"], height=_CFG["height"], startx=_CFG["leftright"], starty=_CFG["topbottom"])

title(titlestring) - устанавливает заголовок окна
